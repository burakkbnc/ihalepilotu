import { requireAdminPermission } from '@/lib/auth/adminGuard';
import { Settings } from 'lucide-react';
export default async function Page(){await requireAdminPermission('settings');return <div className="mx-auto w-full max-w-[1200px] space-y-6"><section className="rounded-[34px] border border-slate-200 bg-white p-8 shadow-sm"><div className="inline-flex items-center gap-2 rounded-full bg-slate-100 px-4 py-2 text-xs font-bold uppercase tracking-[.18em] text-slate-700"><Settings size={14}/>Sistem</div><h1 className="mt-5 text-4xl font-semibold">Ayarlar</h1><p className="mt-3 text-slate-600">Sistem ayarları için güvenli yönetim alanı. API anahtarları bu ekranda gösterilmez; Vercel Environment Variables üzerinden yönetilir.</p></section><section className="rounded-[28px] border border-slate-200 bg-white p-7"><h2 className="text-xl font-semibold">Üretim güvenliği</h2><div className="mt-5 grid gap-3 md:grid-cols-2"><div className="rounded-2xl bg-slate-50 p-4"><p className="text-sm text-slate-500">LLM Provider</p><p className="mt-1 font-semibold">{process.env.LLM_PROVIDER||'Tanımsız'}</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-sm text-slate-500">Mock LLM</p><p className="mt-1 font-semibold">{process.env.ALLOW_MOCK_LLM_IN_PRODUCTION==='true'?'Açık':'Kapalı'}</p></div></div></section></div>}
